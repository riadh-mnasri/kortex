# Passphrase required by /api/progress. Choose a long private sentence
# and enter the same value in Settings > Cross-device sync in the app.
KORTEX_PASSPHRASE=

# Neon Postgres connection string (Vercel > Storage > Neon).
# When absent, the dev server stores progress in data/progress.json.
DATABASE_URL=
